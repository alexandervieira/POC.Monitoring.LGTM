# Exemplo que utilizei como base para este Docker Compose:
# https://github.com/grafana/adventure/blob/main/docker-compose.yml
services:
  alloy:
    image: grafana/alloy:v1.7.5
    container_name: alloy
    ports:
      - 12345:12345
      - 4318:4318
      - 4317:4317
    volumes:
      - ./config.alloy:/etc/alloy/config.alloy
    command: run --server.http.listen-addr=0.0.0.0:12345 --storage.path=/var/lib/alloy/data /etc/alloy/config.alloy
    depends_on:
      - loki

  prometheus:
    image: prom/prometheus:v3.1.0
    container_name: prometheus
    ports:
      - 9090:9090
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    command: --config.file=/etc/prometheus/prometheus.yml --web.enable-otlp-receiver --enable-feature=exemplar-storage

  loki:
    image: grafana/loki:3.4.2
    container_name: loki
    ports:
      - "3100:3100"
    volumes:
      - ./loki-config.yaml:/etc/loki/local-config.yaml
    command: -config.file=/etc/loki/local-config.yaml

  grafana:
    image: grafana/grafana:11.5.2
    container_name: grafana
    environment:
      # Autenticação
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
      - GF_AUTH_BASIC_ENABLED=false
      - GF_FEATURE_TOGGLES_ENABLE=accessControlOnCall
      
      # Configurações de servidor - CRÍTICO para resolver o erro
      - GF_SERVER_ROOT_URL=http://localhost:3000/grafana  # URL completa com subpath
      - GF_SERVER_SERVE_FROM_SUB_PATH=true                # Forçar servir de subpath
      - GF_SERVER_DOMAIN=localhost
      - GF_SERVER_HTTP_PORT=3000
      - GF_SERVER_PROTOCOL=http
      - GF_SERVER_ENABLE_GZIP=true
      - GF_SERVER_STATIC_ROOT=public
      
      # Embedding
      - GF_SERVER_ALLOW_EMBEDDING=true
      - GF_SECURITY_ALLOW_EMBEDDING=true
      
      # CORS
      - GF_SERVER_CORS_ALLOW_CREDENTIALS=true
      - GF_SERVER_CORS_ALLOW_ORIGINS=http://localhost:4000,http://localhost:3000,http://localhost:5173
      - GF_SERVER_CORS_ALLOW_METHODS=GET,POST,OPTIONS
      - GF_SERVER_CORS_ALLOW_HEADERS=*,Content-Type,Accept,Authorization,X-Requested-With
      
      # Logging
      - GF_LOG_LEVEL=debug
      - GF_LOG_CONSOLE_FORMAT=json
      
      # Desabilitar alertas
      - GF_ALERTING_ENABLED=false
      - GF_UNIFIED_ALERTING_ENABLED=false
      
      # Métricas
      - GF_METRICS_ENABLED=false
      
    ports:
      - "3000:3000"
    volumes:
      - ./grafana-data:/var/lib/grafana      
      - ./grafana-datasources.yaml:/etc/grafana/provisioning/datasources/datasources.yaml
      - ./grafana/provisioning:/etc/grafana/provisioning
      # Adicionar esta linha para os dashboards
      - ./grafana/provisioning/dashboards:/etc/grafana/provisioning/dashboards
      - ./grafana/config/grafana.ini:/etc/grafana/grafana.ini  # Arquivo de configuração
    restart: unless-stopped
#   networks:
#      - monitoring

# Tempo
  # Tempo runs as user 10001, and docker compose creates the volume as root.
  # As such, we need to chown the volume in order for Tempo to start correctly.
  init:
    image: &tempoImage grafana/tempo:2.7.1
    container_name: init
    user: root
    entrypoint:
      - "chown"
      - "10001:10001"
      - "/var/tempo"
    volumes:
      - ./tempo-data:/var/tempo

  memcached:
    image: memcached:1.6.29
    container_name: memcached
    ports:
      - "11211:11211"
    environment:
      - MEMCACHED_MAX_MEMORY=64m  # Set the maximum memory usage
      - MEMCACHED_THREADS=4       # Number of threads to use

  tempo:
    image: *tempoImage
    container_name: tempo
    command: [ "-config.file=/etc/tempo.yaml" ]
    volumes:
      - ./tempo.yaml:/etc/tempo.yaml
    ports:
      - "3200:3200"   # tempo
    depends_on:
      - init
      - memcached

  postgres:
    image: postgres
    container_name: ipostgrest
    volumes:
      - ./BaseContagemPostgreSql.sql:/docker-entrypoint-initdb.d/1-basecontagem.sql
    environment:
      POSTGRES_PASSWORD: "Postgres2025!"
    ports:
      - "5432:5432"